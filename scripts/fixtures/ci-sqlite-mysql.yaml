# CI-only config for the sqlite → mysql cross-engine integration test (#291).
# Loaded by the nightly matrix in .github/workflows/integration-nightly.yml
# via scripts/integration-test-pair.sh --pair sqlite-mysql.
#
# Local reproduction:
#   make build && make mysql-bench-up
#   ./scripts/integration-test-pair.sh --pair sqlite-mysql
# (the sqlite-source fixture is loaded into the .db file by the runner)

source:
  type: sqlite
  database: ${SQLITE_SOURCE_DB}

target:
  type: mysql
  host: localhost
  port: 3306
  database: so2010_minimal_ci
  user: root
  password: ${MYSQL_ROOT_PASSWORD}

migration:
  create_indexes: false
  create_foreign_keys: false
  create_check_constraints: false
  target_mode: drop_recreate
  runtime_tuning: false
  chunk_size: 10
  checkpoint_frequency: 1
  data_dir: ${DMT_STATE_DIR}
